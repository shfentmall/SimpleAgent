# 客户端 UI 设计文档去除本机用户名

- 日期：2026-09-17
- 对比基线：`65d3068`（Add changelog for workbench spaces and local API）
- 对应里程碑：无（文档修正，不属于具体里程碑）

## 功能变化

- 修复：`docs/design/client-ui.md` 里的两处路径示例改用 `~` 表示家目录，不再暴露本机用户名。界面示意图里面包屑的路径、5.2 节 `space.toml` 示例里的 `cwd` 都改成了 `~/...` 形式。runner 读取 agent 空间的 `cwd` 时会做 `expanduser()`，所以示例配置仍然可用，不影响功能。

## 函数级改动

`src/` 下没有改动。

## 配置与依赖

无变化，用户无需手动处理。

## 测试

本次只改了一份设计文档，没有代码改动，因此没有跑测试（工作区里还有另一批未提交的开发中改动，会影响测试结果，故未在此次记录范围内执行）。上一次推送（`65d3068`）时的结果：`pytest` 177 passed，`ruff check` 通过。

## 相关笔记

无
